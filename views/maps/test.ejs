<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tower Defense</title>

    <style>
        #mainPage {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgb(68, 68, 68);
            overflow-x: hidden;
            overflow-y: hidden;
        }

        #gameBoard {
            position: absolute;
            background-color: black;
            top: 0;
            left: 0;
            width: 84.1vw;
            height: calc(100vw / 2);
            z-index: 0;
        }

        #gameShopMenu {
            position: absolute;
            background-color: dimgray;
            top: 0;
            right: 0;
            width: 15.9vw;
            height: calc(100vw / 2);
            z-index: 0;
        }

        #programMenu {
            position: absolute;
            background-color: grey;
            border: none;
            top: 62%;
            left: 0%;
            width: 100%;
            height: 0%;
            z-index: 1;
            max-width: 50px;
        }

        #towerMenu {
            position: absolute;
            background-color: grey;
            display: block;
            top: 6vw;
            left: -15vw;
            width: 15vw;
            height: 34vw;
            opacity: 0.5;
            z-index: 1;
            border-top-right-radius: 15px;
            border-bottom-right-radius: 15px;
        }

        #baseHealth, #money, #wave {
            position: relative;
            color: white;
            z-index: 1;
            top: 0;
            left: 1%;
            display: inline-block;
            margin-right: 10px;
        }

    </style>
</head>

<body>
    <div id="mainPage">
        <p id="baseHealth">Health: </p>
        <p id="money">Bitpogs: </p>
        <p id="wave">Wave: </p>
        <canvas id="gameBoard">
        </canvas>
        <div id="gameShopMenu"></div>
        <div id="towerMenu">
            <div id="programMenu">
                <textarea id="programBox" name="programBox" cols="22" rows="10" style="resize: none; font-family: monospace; white-space: pre; overflow-x: auto;"></textarea>
                <button onclick="getProgram()"></button>
            </div>
        </div>
    </div>
    
    <script src="/socket.io/socket.io.js"></script>
    <script src="gameclientside.js"></script>
</body>

</html>